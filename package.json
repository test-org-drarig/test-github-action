{
  "name": "test-github-action",
  "packageManager": "yarn@3.5.0",
  "version": "0.4.0",
  "scripts": {
    "build": "echo build >> index.js",
    "package": "mkdir -p ./dist && cp index.js dist/index.js"
  },
  "dependencies": {
    "@datadog/datadog-ci": "^2.7.0"
  }
}
